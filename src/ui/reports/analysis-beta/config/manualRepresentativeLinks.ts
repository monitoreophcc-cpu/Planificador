import type { ManualRepresentativeLink } from '@/ui/reports/analysis-beta/services/representative-link.service';

/**
 * Vinculación manual temporal para representantes del reporte.
 *
 * Uso:
 * - agentName: nombre tal como llega en transacciones (columna registro/agente).
 * - representativeName: nombre exacto del representante registrado en el sistema.
 */
export const MANUAL_REPRESENTATIVE_LINKS: ManualRepresentativeLink[] = [];

