/**
 * Legacy placeholder retained only because the filesystem did not allow
 * removing the original file during cleanup. The reference guide was moved out
 * of the runtime path and this file intentionally contains no executable code.
 */
export {}
